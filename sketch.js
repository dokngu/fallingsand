class Grid{
    initialize(width, height){
        this.width = width;
        this.height = height;
        this.grid = new Array(width * height).fill(0);
    }

    // clear the canvas
    clear(){

    }

    // set a particle
    set(color){

    }

    // swap spaces between particles
    swap(a,b){

    }

    // check if space is empty
    isEmpty(){
        return 0;
    }
}